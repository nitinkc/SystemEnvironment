# Mac Terminal is started as interactive login shell thus .bashrc is ignored

# .bash_profile ->  login shell 
# .bashrc -> non-login shell. 
# In reality, it doesn't usually matter and placing configuration in is acceptable.
# I prefer .profile or .bash_profile